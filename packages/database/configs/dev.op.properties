# path to changelog files
searchPath=packages/database
changeLogFile=changelogs.yaml

# This is so the changelog table are not created in the public schema
liquibaseSchemaName=db_changelog
includeSchema=true

# To: DEV
liquibase.command.url=jdbc:postgresql://{{ op://MCESC RSD Config/RSD Dev PostgreSQL/server }}:{{ op://MCESC RSD Config/RSD Dev PostgreSQL/port }}/{{ op://MCESC RSD Config/RSD Dev PostgreSQL/database }}
liquibase.command.username=op://MCESC RSD Config/RSD Dev PostgreSQL/username
liquibase.command.password=op://MCESC RSD Config/RSD Dev PostgreSQL/password

## Add your free Hub API key here
# liquibase.hub.apikey:
# liquibase.hub.mode:all
liquibase.hub.mode=off
