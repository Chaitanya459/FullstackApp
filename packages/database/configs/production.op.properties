# path to changelog files
searchPath=packages/database
changeLogFile=changelogs.yaml

# This is so the changelog table are not created in the public schema
liquibaseSchemaName=db_changelog
includeSchema=true

# To: PRODUCTION
liquibase.command.url=jdbc:postgresql://{{op://MCESC RSD Prod Config/RSD Production PostgreSQL/server}}:{{op://MCESC RSD Prod Config/RSD Production PostgreSQL/port}}/{{op://MCESC RSD Prod Config/RSD Production PostgreSQL/database}}
liquibase.command.username=op://MCESC RSD Prod Config/RSD Production PostgreSQL/username
liquibase.command.password=op://MCESC RSD Prod Config/RSD Production PostgreSQL/password

## Add your free Hub API key here
# liquibase.hub.apikey:
# liquibase.hub.mode:all
liquibase.hub.mode=off
